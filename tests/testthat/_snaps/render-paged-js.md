# render_pagedjs_pdf fails as expected

    Code
      render_pagedjs_pdf(file_path = character())
    Condition
      Error in `if (!nzchar(file_path) || !file.exists(file_path)) ...`:
      ! missing value where TRUE/FALSE needed

