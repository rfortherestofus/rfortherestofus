# Get Blog Posts

Get Blog Posts

## Usage

``` r
get_blog_posts(statamic_token = Sys.getenv("STATAMIC_TOKEN"))
```

## Arguments

- statamic_token:

  defaults to `Sys.getenv("STATAMIC_TOKEN")`

## Value

data frame with all blog posts
